# reduce-all

implement those use `reduce`
```
  map,
  filter,
  every,
  some,
  find,
  findIndex,
  flatMap,
  mapFlat,
  reverse,
  join,
  indexOf,
  chunk,
  groupBy,
  partition,
  countBy,
  includes,
```

